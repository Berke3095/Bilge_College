@model List<Teacher>
@{

}

<div class="container">
    <ul>
        @foreach(var item in Model)
        {
            <li>@item.FirstName @item.LastName @item.Email @item.TCK</li>
        }
    </ul>
</div>
